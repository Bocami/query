namespace Bocami.Practices.Query
{
    /// <summary>
    /// A query queries the state of an application and returns a query result. Queries should never change the state of an application.
    /// </summary>
    public interface IQuery
    {
    }
}
